sudo: required
services:
  - docker

before_install:
  - docker build -t nagib/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run nagib/react-test npm test -- --coverage

after_success:
  - docker build -t nagib/multi-client ./client
  - docker build -t nagib/multi-nginx ./nginx
  - docker build -t nagib/multi-server ./server
  - docker build -t nagib/multi-worker ./worker
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  - docker push nagib/multi-client
  - docker push nagib/multi-nginx
  - docker push nagib/multi-server
  - docker push nagib/multi-worker